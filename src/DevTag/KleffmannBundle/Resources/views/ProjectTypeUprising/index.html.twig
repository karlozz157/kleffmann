{% extends '::base.html.twig' %}

{% block title %}{{ 'Project Type Uprisings'|trans }}{% endblock %}
{% block subtitle %}{{ 'Project Type Uprisings List'|trans }}{% endblock %}

{% block content %}
    <div>
        <div class="header">
            <div class="actions"> <a href="#" class="minimize"><i class="fa fa-chevron-down"></i></a> <a href="#" class="refresh"><i class="fa fa-repeat"></i></a> <a href="#" class="close-down"><i class="fa fa-times"></i></a> </div>
            <h3 class="content-header">{{ block('subtitle') }}</h3>
        </div>
        <div class="porlets-content">
            <p>{{ util.button_text('devtag_kleffmann_projecttypeuprising_new', {}, 'Add New'|trans, 'info') }}</p>
            <div class="table-responsive">
                <table class="table table-bordered table-striped">
                    <thead>
                    <tr>
                        <th>{{ 'Name'|trans }}</th>
                        <th>{{ 'Description'|trans }}</th>
                        <th>{{ 'Actions'|trans }}</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for projectTypeUprising in data %}
                        <tr>
                            <td>{{ projectTypeUprising.name }}</td>
                            <td>{{ projectTypeUprising.description }}</td>
                            <td>
                                {{ util.button_icon('devtag_kleffmann_projecttypeuprising_edit', {id: projectTypeUprising.id}, 'Edit Project Type Uprising'|trans, 'warning', 'pencil') }}
                                {{ util.button_icon('devtag_kleffmann_projecttypeuprising_delete', {id: projectTypeUprising.id}, 'Delete Project Type Uprising'|trans, 'danger', 'remove') }}
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div><!--/table-responsive-->
        </div><!--/porlets-content-->
    </div>
    <div>
        {{ knp_pagination_render(data) }}
    </div>
{% endblock %}
