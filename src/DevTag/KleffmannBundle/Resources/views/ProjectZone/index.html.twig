{% extends '::base.html.twig' %}

{% block title %}{{ 'Project Zones to'|trans }} {{ project.name|title }}{% endblock %}
{% block subtitle %}{{ 'Project Zones List'|trans }}{% endblock %}

{% block content %}
    <div>
        <div class="header">
            <div class="actions"> <a href="#" class="minimize"><i class="fa fa-chevron-down"></i></a> <a href="#" class="refresh"><i class="fa fa-repeat"></i></a> <a href="#" class="close-down"><i class="fa fa-times"></i></a> </div>
            <h3 class="content-header">{{ block('subtitle') }}</h3>
        </div>
        <div class="porlets-content">
            <p>{{ util.button_text('project_zones_new', {project: project.id}, 'Add New'|trans, 'info') }}</p>
            <div class="table-responsive">
                <table class="table table-bordered table-striped">
                    <thead>
                    <tr>
                        <th>{{ 'State'|trans }}</th>
                        <th>{{ 'City'|trans }}</th>
                        <th>{{ 'Actions'|trans }}</th>
                    </tr>
                    </thead>
                    <tbody>
                        {% for projectZone in projectZones %}
                            <tr>
                                <td>{{ projectZone.state.name }}</td>
                                <td>{{ projectZone.city.name }}</td>
                                <td>
                                    {{ util.button_icon('project_zones_edit', {id: projectZone.id}, 'Edit Project Zone'|trans, 'warning', 'pencil') }}
                                    {{ util.button_icon('project_zones_delete', {id: projectZone.id}, 'Delete Project Zone'|trans, 'danger', 'remove') }}
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div><!--/table-responsive-->
        </div><!--/porlets-content-->
    </div>
{% endblock %}
